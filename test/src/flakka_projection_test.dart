// ignore_for_file: prefer_const_constructors
import 'package:flakka_projection/flakka_projection.dart';
import 'package:test/test.dart';

void main() {
  group('FlakkaProjection', () {
    test('can be instantiated', () {
      expect(FlakkaProjection(), isNotNull);
    });
  });
}
